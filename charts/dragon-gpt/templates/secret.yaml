apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dragon-gpt.fullname" . }}-secrets
  labels:
    {{- include "dragon-gpt.labels" . | nindent 4 }}
type: Opaque
stringData:
  OPENAI_API_KEY: {{ .Values.secrets.openaiApiKey | quote }}
