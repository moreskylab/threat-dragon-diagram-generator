apiVersion: v1
kind: Service
metadata:
  name: {{ include "dragon-gpt.fullname" . }}
  labels:
    {{- include "dragon-gpt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dragon-gpt.selectorLabels" . | nindent 4 }}
